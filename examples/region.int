;; -*- mode: lisp -*-

(defun main () i32
  (letregion rho
    (let ((p (allocate rho 10)))
      (letregion rho'
        (let ((p' (allocate rho' 12)))
          ;; Will not work:
          ;;(<- p p2)
          0)))))
